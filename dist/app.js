"use strict";
/*$(document).ready(function(){
    let submitGetB = $("#submitGetB");
    let getB_aX = $("#getB_aX");
    let getB_aY = $("#getB_aY");
    
    if(submitGetB) {
        submitGetB.on('click', getB(getB_aX.val(), getB_aY.val(),getB_aX.val(), getB_aY.val()));
    }
    
    function getB(aX, aY, abX, abY) {
        let x = (aX + abX);
        let y = (aY + abY);

        console.log( "" + x + ", " + y);
    }
});
*/ 
//# sourceMappingURL=app.js.map